machine:
  node:
    version: 4.4.5

dependencies:
  pre:
    - git config user.email "support@jsconf.cn"
    - git config user.name "jsconfcn"
    - npm i npm@^3.9.0 -g

deployment:
  deploy:
    branch: master
    commands:
      - npm run deploy

